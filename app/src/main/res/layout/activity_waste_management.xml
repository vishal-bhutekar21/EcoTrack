<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp">

    <com.google.android.material.chip.ChipGroup
        android:id="@+id/chipGroup"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:singleSelection="true">

        <com.google.android.material.chip.Chip
            android:id="@+id/chipNoAction"
            android:text="No Action Needed"  android:layout_height="wrap_content" android:layout_width="100dp"/>

        <com.google.android.material.chip.Chip
            android:id="@+id/chipYetToExpire"
            android:text="Yet to be Expired"  android:layout_height="wrap_content" android:layout_width="100dp"/>

        <com.google.android.material.chip.Chip
            android:id="@+id/chipExpired"
            android:text="Expired"  android:layout_height="wrap_content" android:layout_width="100dp"/>
    </com.google.android.material.chip.ChipGroup>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="10dp"/>
</LinearLayout>
